import { Metadata } from '@contentstack/utils/dist/types/Models/metadata-model';
declare global {
    interface String {
        forEachEmbeddedItem(callbackfn: (embededObjectTag: string, object: Metadata) => void): void;
    }
}
